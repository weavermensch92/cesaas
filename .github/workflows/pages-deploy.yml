name: pages-deploy

# V_Voice/dealer/demo/index.html을 GitHub Pages에 publish.
# fly.io org token이 SSO로 막힌 환경에서 데모 페이지 즉시 보여주기 위한 우회.
#
# 1회 setup (위버 클릭 1번):
#   https://github.com/weavermensch92/cesaas/settings/pages
#   → Source: GitHub Actions 선택 → Save
#
# 이후 main 머지 시 자동 publish.
# URL: https://weavermensch92.github.io/cesaas/

on:
  push:
    branches: [main]
    paths:
      - 'V_Voice/dealer/demo/**'
      - 'V_Voice/dealer/v2mobile/**'
      - '.github/workflows/pages-deploy.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Stage public files
        run: |
          mkdir -p _site
          cp V_Voice/dealer/demo/index.html _site/index.html
          # v2mobile — 모바일 우선 dealer UI 개발 페이지. /v2mobile/ 로 접근.
          mkdir -p _site/v2mobile
          cp V_Voice/dealer/v2mobile/index.html _site/v2mobile/index.html
          cat > _site/README.md <<EOF
          # HD건설기계 — Dealer Demo

          - / — \`V_Voice/dealer/demo/index.html\` (현재 운영 데모)
          - /v2mobile/ — \`V_Voice/dealer/v2mobile/index.html\` (모바일 우선 v2 — 스크롤 fix 검증용)
          DB 미연결. CTT Expo 2026 Dealer 인터뷰 폼.
          EOF
      - uses: actions/configure-pages@v5
        with:
          enablement: true  # Pages 자동 활성화 시도 (Settings 1 클릭 우회)
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
